export default function Footer() {
  const styles = {
    gridArea: "footer",
    // border: "3px solid yellow",
    textAlign: "left",
    padding: "0.3rem",
  };

  return <footer style={styles}></footer>;
}
